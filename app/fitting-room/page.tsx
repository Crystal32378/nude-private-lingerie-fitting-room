import { FittingRoomView } from "@/components/views/fitting-room";

export const metadata = { title: "NUDE — Private Fitting Room" };

export default function FittingRoomPage() {
  return <FittingRoomView />;
}
